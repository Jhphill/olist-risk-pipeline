version: 2

sources:
  # ── Mes 1: carga batch original (raw_*) ──────────────────────
  - name: public
    database: olist_db
    schema: public
    tables:
      - name: raw_orders
      - name: raw_customers
      - name: raw_order_items
      - name: raw_order_payments
      - name: raw_order_reviews
      - name: raw_products
      - name: raw_sellers
      - name: raw_geolocation
      - name: raw_product_category_translation

  # ── Mes 2: streaming (Kafka → Spark → bronze.*) ──────────────
  - name: bronze
    database: olist_db
    schema: bronze
    tables:
      - name: orders
      - name: payments
      - name: reviews
